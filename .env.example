# Required: JWT signing secret for authentication
SECRET=your-jwt-secret-here

# Optional: Node environment ("production" for production logging + secure cookies)
NODE_ENV=development

# Optional: Server port (default: 3000)
PORT=3000

# Optional: SQLite database path (default: ./data/hackernews.db)
SQLITE_PATH=./data/hackernews.db
