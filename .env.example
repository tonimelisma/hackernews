# Required: JWT signing secret for authentication
SECRET=your-jwt-secret-here

# Optional: Node environment (affects Firestore collection prefix)
# "production" → prod-stories/prod-users
# "ci" → ci-stories/ci-users
# anything else → dev-stories/dev-users
NODE_ENV=development

# Optional: Server port (default: 3000)
PORT=3000
